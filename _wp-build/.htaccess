# Nega acesso web a scripts de build/snapshots (o repo faz deploy no public_html do site estatico).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
