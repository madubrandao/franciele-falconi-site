.landing {
  --ff-bg: #f1ece1;
  --ff-green: #3c4a34;
  --ff-green-dark: #2b3524;
  --ff-bg-alt: #e7e0cf;
  --ff-gold: #cb9d50;
  --ff-gold-light: #d9b877;
  --ff-hero-light: #c8e4b8;
  --ff-secondary: #6b7059;
  --ff-light: #ece5d3;
  --ff-muted-green: #8a9478;
  --ff-container: 1220px;
  --ff-hero-container: 1320px;
  --ff-nav-offset: 82px;
  --ff-shadow-soft: 0 24px 50px rgba(47, 50, 29, 0.16);
  --ff-shadow-deep: 0 28px 60px rgba(47, 50, 29, 0.18);

  min-height: 100vh;
  background: var(--ff-bg);
  color: var(--ff-green);
  font-family: var(--font-montserrat), ui-sans-serif, system-ui, sans-serif;
  overflow-x: clip;
  scroll-behavior: smooth;
}

.landing *,
.landing *::before,
.landing *::after {
  box-sizing: border-box;
}

.landing ::selection {
  background: var(--ff-gold);
  color: #fff;
}

.landing a {
  color: #8a6a2f;
  text-decoration: none;
}

.landing a,
.landing button {
  -webkit-tap-highlight-color: transparent;
}

.landing :where(a, button):focus-visible {
  outline: 2px solid var(--ff-gold-light);
  outline-offset: 5px;
}

.landing :where(section, header, footer, [id]) {
  scroll-margin-top: var(--ff-nav-offset);
}

.landing[data-enhanced="true"] [data-reveal] {
  opacity: 0;
  transform: translateY(28px) scale(0.98);
  transition:
    opacity 0.9s ease var(--reveal-delay, 0ms),
    transform 0.9s cubic-bezier(0.22, 0.7, 0.2, 1) var(--reveal-delay, 0ms);
}

.landing[data-enhanced="true"] [data-reveal].revealVisible {
  opacity: 1;
  transform: none;
}

.progress {
  position: fixed;
  top: 0;
  left: 0;
  z-index: 70;
  width: 0%;
  height: 3px;
  background: linear-gradient(90deg, var(--ff-gold-light), var(--ff-gold));
  pointer-events: none;
  transition: width 0.12s linear;
}

.nav {
  position: fixed;
  top: 0;
  right: 0;
  left: 0;
  z-index: 60;
  display: flex;
  justify-content: center;
  padding: clamp(14px, 2vw, 24px) clamp(16px, 3vw, 32px);
  pointer-events: none;
}

.navPill {
  display: flex;
  align-items: center;
  gap: clamp(14px, 2.4vw, 34px);
  width: 100%;
  max-width: 920px;
  padding: 10px 10px 10px clamp(16px, 2vw, 22px);
  border: 1px solid rgba(246, 241, 228, 0.2);
  border-radius: 999px;
  background: var(--ff-green);
  box-shadow: 0 16px 40px rgba(12, 16, 8, 0.4);
  pointer-events: auto;
  transition:
    background 0.4s,
    box-shadow 0.4s;
}

.nav.navScrolled .navPill {
  background: var(--ff-green-dark);
  box-shadow: 0 18px 44px rgba(12, 16, 8, 0.55);
}

a.navBrand {
  display: flex;
  flex: none;
  align-items: center;
  gap: 10px;
  color: var(--ff-light);
  font-family: var(--font-playfair-display), Georgia, serif;
  font-size: clamp(13px, 1.1vw, 15px);
  letter-spacing: 0.02em;
}

.navBrand img {
  display: block;
  width: clamp(28px, 3vw, 34px);
  height: auto;
  border-radius: 50%;
  object-fit: cover;
}

.navMenu {
  display: flex;
  align-items: center;
  gap: clamp(12px, 1.8vw, 26px);
  margin-right: auto;
  min-width: 0;
}

a.navLink {
  color: rgba(246, 241, 228, 0.72);
  font-size: clamp(10.5px, 0.86vw, 12px);
  font-weight: 500;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  white-space: nowrap;
  transition: color 0.25s;
}

a.navLink:hover {
  color: var(--ff-gold-light);
}

a.navPillCta {
  display: inline-flex;
  flex: none;
  min-height: 40px;
  align-items: center;
  justify-content: center;
  padding: 0 clamp(18px, 2vw, 24px);
  border-radius: 999px;
  background: var(--ff-light);
  color: var(--ff-green);
  font-size: clamp(10.5px, 0.9vw, 12px);
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  white-space: nowrap;
  transition:
    transform 0.3s,
    background 0.3s;
}

a.navPillCta:hover {
  transform: translateY(-1px);
  background: #fff;
}

.primaryButton,
.secondaryButton {
  display: inline-flex;
  min-height: 44px;
  align-items: center;
  justify-content: center;
  border-radius: 999px;
  font-size: clamp(10.5px, 0.9vw, 12px);
  font-weight: 600;
  letter-spacing: 0.12em;
  line-height: 1;
  text-align: center;
  white-space: nowrap;
  transition:
    transform 0.3s,
    border-color 0.3s,
    background 0.3s,
    color 0.3s,
    box-shadow 0.3s;
}

.primaryButton {
  animation: ffBtnPulse 2.6s ease-in-out infinite;
  background: linear-gradient(135deg, var(--ff-gold-light), var(--ff-gold));
  box-shadow: 0 8px 24px rgba(203, 157, 80, 0.3);
  color: var(--ff-green);
  padding: clamp(13px, 1.5vw, 16px) clamp(22px, 2.6vw, 32px);
}

.primaryButton:hover {
  animation: none;
  color: var(--ff-green);
  transform: translateY(-2px);
}

.secondaryButton {
  border: 1px solid rgba(236, 229, 211, 0.42);
  color: #dcdfd0;
  padding: clamp(13px, 1.5vw, 16px) clamp(22px, 2.6vw, 32px);
}

.secondaryButton:hover {
  border-color: var(--ff-gold-light);
  background: rgba(236, 229, 211, 0.08);
  color: var(--ff-light);
}

/* ===== HERO — modelo Supaste adaptado aos tons da marca ===== */
.hero {
  position: relative;
  min-height: 165vh;
  background: linear-gradient(
    180deg,
    #1b2217 0%,
    var(--ff-green) 30%,
    #6f7d5a 58%,
    var(--ff-bg) 100%
  );
}

.heroSticky {
  position: sticky;
  top: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100svh;
  overflow: hidden;
  padding: clamp(96px, 14vh, 140px) clamp(20px, 4vw, 40px);
}

.heroBackdrop {
  position: absolute;
  inset: 0;
  z-index: 0;
  pointer-events: none;
  background: radial-gradient(
    120% 60% at 50% 0%,
    rgba(246, 241, 228, 0.16),
    rgba(246, 241, 228, 0) 60%
  );
}

.heroHills {
  position: absolute;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 1;
  width: 100%;
  height: clamp(160px, 28vh, 300px);
  pointer-events: none;
}

.leafOrnament {
  display: block;
  pointer-events: none;
}

.leafHorizontal {
  width: clamp(80px, 8vw, 120px);
}

.heroContent {
  position: relative;
  z-index: 3;
  display: flex;
  flex-direction: column;
  align-items: center;
  max-width: 820px;
  text-align: center;
  will-change: opacity, transform;
  opacity: clamp(0, calc(1 - var(--hero-progress, 0) * 2), 1);
  transform: translateY(calc(var(--hero-progress, 0) * -12vh));
}

.heroEyebrow {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin-bottom: clamp(18px, 3vh, 28px);
  color: var(--ff-light);
  font-family: var(--font-cinzel), Georgia, serif;
  font-size: clamp(10.5px, 1vw, 13px);
  font-weight: 600;
  letter-spacing: 0.28em;
  text-transform: uppercase;
}

.heroTitle {
  margin: 0;
  font-weight: 400;
  letter-spacing: -0.02em;
  line-height: 1;
}

.heroTitleStrong,
.heroTitleScript {
  display: block;
  font-size: clamp(42px, min(8.6vw, 12vh), 120px);
}

.heroTitleStrong {
  color: var(--ff-light);
  font-family: var(--font-montserrat), ui-sans-serif, system-ui, sans-serif;
  font-weight: 600;
}

.heroTitleScript {
  color: var(--ff-gold-light);
  font-family: var(--font-playfair-display), Georgia, serif;
  font-style: italic;
  font-weight: 400;
}

.heroLead {
  max-width: 40em;
  margin: clamp(20px, 3vh, 30px) 0 0;
  color: rgba(246, 241, 228, 0.82);
  font-size: clamp(14px, 1.3vw, 18px);
  line-height: 1.6;
}

.heroCtaRow {
  margin-top: clamp(24px, 4vh, 38px);
}

a.heroCta {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  min-height: 54px;
  padding: 0 clamp(26px, 3vw, 36px);
  border-radius: 999px;
  background: #12180e;
  border: 1px solid rgba(246, 241, 228, 0.14);
  box-shadow: 0 18px 40px rgba(10, 14, 8, 0.45);
  color: var(--ff-light);
  font-size: clamp(12px, 1vw, 14px);
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  transition:
    transform 0.3s,
    background 0.3s;
}

a.heroCta:hover {
  transform: translateY(-2px);
  background: #1c2417;
}

.heroBadges {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: clamp(14px, 2vw, 26px);
  margin-top: clamp(18px, 2.6vh, 24px);
  color: rgba(246, 241, 228, 0.6);
  font-size: clamp(10.5px, 0.9vw, 12px);
  letter-spacing: 0.08em;
}

/* ---- Mockup da janela do app (2ª seção que sobe na rolagem) ---- */
.heroMockup {
  position: absolute;
  left: 50%;
  top: 50%;
  z-index: 2;
  width: min(1100px, 94vw);
  will-change: transform, opacity;
  transform: translate(
      -50%,
      calc(-50% + (1 - var(--hero-progress, 0)) * 72vh)
    )
    scale(calc(0.9 + var(--hero-progress, 0) * 0.1));
  opacity: clamp(0, calc(0.12 + var(--hero-progress, 0) * 1.4), 1);
}

.mockupFrame {
  padding: clamp(10px, 1.4vw, 18px);
  border: 1px solid rgba(246, 241, 228, 0.45);
  border-radius: clamp(20px, 2.4vw, 34px);
  background: rgba(246, 241, 228, 0.22);
  box-shadow:
    0 40px 90px rgba(20, 26, 15, 0.45),
    inset 0 1px 0 rgba(246, 241, 228, 0.4);
  backdrop-filter: blur(10px);
}

.mockupWindow {
  display: grid;
  grid-template-columns: clamp(112px, 14vw, 168px) minmax(0, 1fr);
  height: clamp(270px, 42vh, 400px);
  border-radius: clamp(14px, 1.8vw, 22px);
  background: #171d13;
  box-shadow: 0 12px 30px rgba(10, 14, 8, 0.35);
  overflow: hidden;
}

.mockupSidebar {
  padding: clamp(14px, 1.6vw, 20px);
  border-right: 1px solid rgba(246, 241, 228, 0.08);
}

.mockupBrand {
  display: flex;
  align-items: center;
  gap: 8px;
  color: rgba(246, 241, 228, 0.86);
  font-family: var(--font-playfair-display), Georgia, serif;
  font-size: clamp(12px, 1vw, 14px);
}

.mockupBrand span {
  width: 16px;
  height: 16px;
  border-radius: 5px;
  background: linear-gradient(135deg, var(--ff-gold-light), var(--ff-gold));
}

.mockupBody {
  display: flex;
  flex-direction: column;
  gap: clamp(12px, 1.6vh, 18px);
  min-width: 0;
  padding: clamp(14px, 1.8vw, 22px);
}

.mockupTopbar {
  display: flex;
  align-items: center;
  gap: 12px;
}

.mockupSearch {
  display: flex;
  flex: 1;
  align-items: center;
  gap: 8px;
  min-width: 0;
  padding: 9px 14px;
  border-radius: 999px;
  background: rgba(246, 241, 228, 0.08);
  color: rgba(246, 241, 228, 0.5);
  font-size: clamp(11px, 0.9vw, 13px);
}

.mockupTools {
  display: flex;
  flex: none;
  gap: 6px;
}

.mockupTools i {
  width: 28px;
  height: 28px;
  border-radius: 50%;
  background: rgba(246, 241, 228, 0.08);
}

.mockupTabs {
  display: flex;
  gap: 6px;
  overflow: hidden;
}

.mockupTabs span {
  flex: none;
  padding: 6px 12px;
  border-radius: 999px;
  background: rgba(246, 241, 228, 0.06);
  color: rgba(246, 241, 228, 0.55);
  font-size: clamp(10px, 0.82vw, 11.5px);
  letter-spacing: 0.04em;
  white-space: nowrap;
}

.mockupTabActive {
  background: var(--ff-light);
  color: var(--ff-green);
  font-weight: 600;
}

.mockupTabAdd {
  width: 26px;
  padding: 6px 0;
  text-align: center;
}

.mockupCards {
  display: grid;
  flex: 1;
  grid-template-columns: repeat(5, minmax(0, 1fr));
  gap: clamp(8px, 1vw, 14px);
  min-height: 0;
}

.mockupCard {
  display: flex;
  flex-direction: column;
  border-radius: 12px;
  background: rgba(246, 241, 228, 0.05);
  overflow: hidden;
}

.mockupCardMedia {
  display: flex;
  flex: 1;
  align-items: center;
  justify-content: center;
  padding: 10px;
  color: rgba(246, 241, 228, 0.85);
  font-size: clamp(9px, 0.8vw, 11px);
  text-align: center;
}

.cardImage {
  background: linear-gradient(135deg, var(--ff-muted-green), #4b512d);
}

.cardNote {
  background: rgba(246, 241, 228, 0.1);
}

.cardShot {
  background: linear-gradient(135deg, #dbcebe, #9d948d);
  color: var(--ff-green);
}

.cardColor {
  background: var(--ff-green);
  color: var(--ff-light);
  font-family: ui-monospace, monospace;
}

.cardIcon {
  background: rgba(246, 241, 228, 0.92);
  color: var(--ff-gold);
  font-size: 16px;
}

.mockupCardFoot {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 7px 8px;
  color: rgba(246, 241, 228, 0.45);
  font-size: 9px;
}

.mockupCardFoot i {
  width: 12px;
  height: 12px;
  flex: none;
  border-radius: 3px;
  background: rgba(246, 241, 228, 0.2);
}

.finalSeal {
  position: relative;
  display: grid;
  flex: none;
  place-items: center;
  aspect-ratio: 1;
  border-radius: 50%;
  background: var(--ff-green);
  text-align: center;
  will-change: transform;
}

.finalSeal svg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  animation: ffSpin 44s linear infinite;
}

.container {
  width: 100%;
  max-width: var(--ff-container);
  margin: 0 auto;
}

.pathsSection {
  background: var(--ff-bg);
  padding: clamp(20px, 3vw, 44px) clamp(24px, 4vw, 56px) clamp(80px, 9vw, 120px);
}

.sectionHeader,
.methodHeader,
.corporateHeader {
  text-align: center;
}

.sectionHeader {
  margin-bottom: clamp(44px, 5vw, 66px);
}

.kicker {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.kickerCenter {
  justify-content: center;
  gap: 16px;
  margin-bottom: 20px;
}

.kicker > span:first-child,
.kickerCenter > span:last-child {
  width: 24px;
  height: 1px;
  background: var(--ff-gold);
  flex: none;
}

.kickerCenter > span:first-child,
.kickerCenter > span:last-child {
  width: 28px;
}

.kicker > span:nth-child(2) {
  color: #8a7a4a;
  font-family: var(--font-cinzel), Georgia, serif;
  font-size: 11.5px;
  font-weight: 500;
  letter-spacing: 0.32em;
  min-width: 0;
}

.kickerCenter > span:nth-child(2) {
  font-size: 12px;
  letter-spacing: 0.34em;
}

.sectionHeader h2,
.methodHeader h2,
.corporateHeader h2 {
  margin: 0;
  color: var(--ff-green);
  font-family: var(--font-playfair-display), Georgia, serif;
  font-weight: 400;
  line-height: 1.08;
  text-wrap: balance;
}

.sectionHeader h2 {
  font-size: clamp(32px, 4.8vw, 62px);
  line-height: 1.04;
}

.sectionHeader em,
.methodHeader em {
  color: var(--ff-gold);
}

.pathsGrid {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
  gap: clamp(30px, 4vw, 64px);
}

.pathCard {
  min-width: 0;
  text-align: center;
}

.pathCard h3 {
  margin: 0;
  color: var(--ff-green);
  font-family: var(--font-playfair-display), Georgia, serif;
  font-size: clamp(20px, 2.1vw, 26px);
  font-weight: 500;
  letter-spacing: 0.08em;
}

.pathCard > span {
  display: block;
  margin: 9px 0 24px;
  color: #8b8f76;
  font-size: 12.5px;
  letter-spacing: 0.06em;
}

.pathImage {
  position: relative;
  overflow: hidden;
  aspect-ratio: 4 / 3.4;
  background: #e2dac6;
  box-shadow: var(--ff-shadow-soft);
}

.pathImageIndividual {
  border-radius: 46% 54% 42% 58% / 8% 8% 44% 44%;
}

.pathImageCompany {
  border-radius: 54% 46% 58% 42% / 8% 8% 44% 44%;
}

.coverImage {
  display: block;
  object-fit: cover;
  transition: transform 0.9s cubic-bezier(0.22, 0.7, 0.2, 1);
}

.pathImageIndividual .coverImage {
  object-position: 50% 30%;
}

.pathImageCompany .coverImage {
  object-position: 50% 32%;
}

.pathImage:hover .coverImage,
.finalImage:hover .coverImage,
.aboutMainImage:hover .coverImage {
  transform: scale(1.035);
}

.pathCard h4 {
  max-width: 420px;
  margin: 30px auto 0;
  color: var(--ff-green);
  font-family: var(--font-playfair-display), Georgia, serif;
  font-size: clamp(21px, 2.3vw, 28px);
  font-weight: 400;
  line-height: 1.28;
  text-wrap: pretty;
}

.pathCard p {
  max-width: 400px;
  margin: 14px auto 0;
  color: var(--ff-secondary);
  font-size: 14.5px;
  font-weight: 300;
  line-height: 1.85;
  text-wrap: pretty;
}

.textLink {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  margin-top: 22px;
  border-bottom: 1px solid var(--ff-gold);
  color: #8a6a2f;
  font-size: 11.5px;
  font-weight: 600;
  letter-spacing: 0.16em;
  line-height: 1.5;
  padding-bottom: 6px;
  text-align: left;
  transition: color 0.25s;
}

.textLink:hover {
  color: var(--ff-gold);
}

.textLink span {
  transition: transform 0.25s, opacity 0.25s;
}

.textLink:hover span {
  opacity: 0.8;
  transform: translateX(7px);
}

.testimonialsSection {
  position: relative;
  background: var(--ff-bg);
  padding: 0 clamp(16px, 3vw, 44px) clamp(70px, 8vw, 100px);
}

.testimonialsLeaf {
  position: absolute;
  top: 8%;
  left: clamp(-30px, -1vw, 0px);
  opacity: 0.22;
}

.testimonialsPanel {
  position: relative;
  max-width: 1180px;
  margin: 0 auto;
  border-radius: 44% 56% 52% 48% / 22% 22% 26% 26%;
  background: var(--ff-green);
  padding: clamp(56px, 6vw, 84px) clamp(30px, 4.5vw, 74px);
}

.testimonialsTitle {
  margin-bottom: clamp(38px, 4vw, 52px);
  color: var(--ff-gold-light);
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.3em;
  text-align: center;
}

.testimonialsGrid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

.testimonial {
  display: flex;
  align-items: flex-start;
  gap: 18px;
  min-width: 0;
  padding: 8px clamp(18px, 2.4vw, 34px);
}

.testimonial + .testimonial {
  border-left: 1px solid rgba(217, 184, 119, 0.26);
}

.avatar {
  display: grid;
  flex: 0 0 auto;
  width: 56px;
  height: 56px;
  place-items: center;
  border-radius: 50%;
  background: rgba(236, 229, 211, 0.1);
  box-shadow: 0 0 0 1px rgba(217, 184, 119, 0.4);
  color: var(--ff-gold-light);
  font-family: var(--font-playfair-display), Georgia, serif;
  font-size: 20px;
}

.testimonial p {
  margin: 0 0 16px;
  color: #e4e6d8;
  font-size: 14.5px;
  font-weight: 300;
  line-height: 1.8;
  text-wrap: pretty;
}

.testimonial strong {
  display: block;
  color: var(--ff-gold-light);
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.14em;
}

.testimonial span {
  display: block;
  margin-top: 5px;
  color: #9aa38c;
  font-size: 10.5px;
  letter-spacing: 0.14em;
}

.serviceSection {
  background: var(--ff-bg);
  padding: clamp(30px, 4vw, 50px) clamp(24px, 4vw, 56px) clamp(80px, 9vw, 120px);
}

.twoColumn {
  display: grid;
  grid-template-columns: minmax(0, 0.72fr) minmax(0, 1.28fr);
  align-items: start;
  gap: clamp(40px, 5vw, 84px);
}

.twoColumn > * {
  min-width: 0;
}

.twoColumn h2 {
  margin: 0 0 18px;
  color: var(--ff-green);
  font-family: var(--font-playfair-display), Georgia, serif;
  font-size: clamp(27px, 3.1vw, 40px);
  font-weight: 400;
  line-height: 1.12;
  text-wrap: balance;
}

.leadText {
  margin: 0 0 34px;
  color: #5f6344;
  font-size: 15px;
  font-weight: 300;
  line-height: 1.85;
  text-wrap: pretty;
}

.featureList {
  display: flex;
  flex-direction: column;
  gap: 26px;
}

.featureItem,
.corporateItem {
  display: flex;
  align-items: flex-start;
  gap: 18px;
  min-width: 0;
}

.roundIcon {
  display: grid;
  flex: 0 0 auto;
  width: 44px;
  height: 44px;
  place-items: center;
  border: 1px solid var(--ff-gold);
  border-radius: 50%;
}

.roundIconFilled {
  border-color: transparent;
  background: var(--ff-gold);
}

.featureItem h4 {
  margin: 11px 0 8px;
  color: var(--ff-green);
  font-size: 12.5px;
  font-weight: 600;
  letter-spacing: 0.13em;
}

.featureItem p,
.corporateItem p {
  margin: 0;
  color: var(--ff-secondary);
  font-size: 14px;
  font-weight: 300;
  line-height: 1.75;
  text-wrap: pretty;
}

.serviceCta {
  margin-top: 38px;
  padding: 15px 30px;
}

.methodBlock {
  min-width: 0;
}

.methodHeader {
  margin-bottom: clamp(34px, 4vw, 50px);
}

.methodHeader h2 {
  font-size: clamp(27px, 3.3vw, 44px);
}

.steps {
  display: flex;
  align-items: flex-start;
  gap: clamp(6px, 1vw, 14px);
  scrollbar-width: none;
}

.steps::-webkit-scrollbar {
  display: none;
}

.stepGroup {
  display: flex;
  flex: 1 1 0;
  align-items: flex-start;
  gap: clamp(6px, 1vw, 14px);
  min-width: 0;
}

.step {
  flex: 1 1 0;
  min-width: 0;
  text-align: center;
}

.stepBody {
  min-width: 0;
}

.stepIcon {
  display: grid;
  width: clamp(58px, 6vw, 74px);
  aspect-ratio: 1;
  place-items: center;
  margin: 0 auto 16px;
  border-radius: 50%;
  background: #e9e2d0;
}

.stepIconFinal {
  background: var(--ff-gold);
}

.stepNumber {
  margin-bottom: 8px;
  color: #a79c7c;
  font-family: var(--font-playfair-display), Georgia, serif;
  font-size: 15px;
}

.step h3 {
  margin: 0 0 10px;
  color: var(--ff-green);
  font-family: var(--font-cinzel), Georgia, serif;
  font-size: 10.5px;
  font-weight: 600;
  letter-spacing: 0.08em;
  line-height: 1.45;
  overflow-wrap: anywhere;
  word-break: break-word;
}

.step p {
  margin: 0;
  color: var(--ff-secondary);
  font-size: 13px;
  font-weight: 300;
  line-height: 1.75;
  overflow-wrap: anywhere;
  text-wrap: pretty;
}

.arrow {
  flex: 0 0 auto;
  align-self: flex-start;
  margin-top: 34px;
  animation: ffDown 2.8s ease-in-out infinite;
  color: var(--ff-gold);
  font-size: 13px;
  opacity: 0.7;
}

.methodText {
  max-width: 620px;
  margin: clamp(34px, 4vw, 48px) 0 0;
  color: var(--ff-secondary);
  font-size: 14.5px;
  font-weight: 300;
  line-height: 1.85;
  text-wrap: pretty;
}

.scriptPhrase {
  margin-top: clamp(26px, 3vw, 40px);
  color: #8a7a4a;
  font-family: var(--font-petit-formal-script), cursive;
  font-size: clamp(22px, 2.3vw, 30px);
  line-height: 1.4;
  text-align: right;
}

.corporateHeader {
  margin-top: clamp(76px, 8vw, 110px);
}

.corporateHeader h2 {
  margin: 0 0 clamp(42px, 4.5vw, 60px);
  font-size: clamp(26px, 3.1vw, 40px);
  line-height: 1.12;
}

.corporateGrid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: clamp(28px, 3vw, 48px) clamp(28px, 3vw, 54px);
}

.corporateItem h4 {
  margin: 8px 0 9px;
  color: var(--ff-green);
  font-family: var(--font-playfair-display), Georgia, serif;
  font-size: 18px;
  font-weight: 500;
}

.aboutSection {
  position: relative;
  background: var(--ff-bg-alt);
  padding: clamp(80px, 9vw, 120px) clamp(24px, 4vw, 56px);
}

.aboutGrid {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
  align-items: center;
  gap: clamp(40px, 5vw, 86px);
}

.aboutMedia {
  position: relative;
  min-width: 0;
}

.aboutOutline {
  position: absolute;
  top: -5%;
  right: -6%;
  bottom: 9%;
  left: 8%;
  border: 1px solid var(--ff-gold);
  border-radius: 52% 48% 44% 56% / 46% 46% 54% 54%;
  pointer-events: none;
  will-change: transform;
}

.aboutMainImage {
  position: relative;
  width: 100%;
  min-height: clamp(320px, 45vw, 560px);
  overflow: hidden;
  aspect-ratio: 5 / 5.2;
  border-radius: 48% 52% 46% 54% / 44% 44% 56% 56%;
  background: #d8d0b8;
  box-shadow: var(--ff-shadow-deep);
}

.aboutMainImage .coverImage {
  object-position: 50% 26%;
}

.aboutCircleImage {
  position: absolute;
  bottom: 2%;
  left: -2%;
  width: clamp(112px, 13vw, 156px);
  aspect-ratio: 1;
  overflow: hidden;
  border-radius: 50%;
  box-shadow:
    0 0 0 6px var(--ff-bg-alt),
    0 16px 36px rgba(47, 50, 29, 0.26);
  will-change: transform;
}

.aboutCircleImage .coverImage {
  object-position: 55% 26%;
}

.aboutCopy {
  min-width: 0;
}

.aboutCopy h2 {
  margin: 0 0 14px;
  color: var(--ff-green);
  font-family: var(--font-playfair-display), Georgia, serif;
  font-size: clamp(28px, 3.6vw, 48px);
  font-weight: 400;
  line-height: 1.12;
  text-wrap: balance;
}

.aboutScript,
.finalScript {
  color: #8a7a4a;
  font-family: var(--font-petit-formal-script), cursive;
  font-size: clamp(22px, 2.3vw, 30px);
}

.aboutScript {
  margin: 0 0 28px;
}

.aboutCopy p {
  margin: 0 0 20px;
  color: #4d5138;
  font-size: 15.5px;
  font-weight: 300;
  line-height: 1.9;
  text-wrap: pretty;
}

.tags {
  display: flex;
  flex-wrap: wrap;
  gap: 9px;
  margin: 10px 0 32px;
}

.tags span {
  border: 1px solid #bcb298;
  border-radius: 999px;
  color: #5f6344;
  font-size: 10.5px;
  font-weight: 500;
  letter-spacing: 0.14em;
  line-height: 1.2;
  padding: 9px 16px;
}

.finalSection {
  position: relative;
  overflow: hidden;
  background: var(--ff-green);
  padding: 0 clamp(24px, 4vw, 56px) clamp(90px, 10vw, 130px);
}

.finalWave {
  position: absolute;
  top: -1px;
  right: 0;
  left: 0;
  display: block;
  width: 100%;
  height: clamp(56px, 7.5vw, 110px);
  pointer-events: none;
}

.finalGlow {
  position: absolute;
  bottom: -12%;
  left: 6%;
  width: min(320px, 42vw);
  aspect-ratio: 1;
  border-radius: 50%;
  background: radial-gradient(circle at 40% 40%, rgba(203, 157, 80, 0.16), rgba(203, 157, 80, 0) 70%);
  pointer-events: none;
  will-change: transform;
}

.finalGrid {
  position: relative;
  display: grid;
  grid-template-columns: minmax(0, 1.06fr) minmax(0, 0.94fr);
  align-items: center;
  gap: clamp(36px, 4vw, 70px);
  padding-top: clamp(96px, 11vw, 150px);
}

.finalCopy {
  min-width: 0;
}

.finalCopy h2 {
  margin: 0 0 16px;
  color: var(--ff-light);
  font-family: var(--font-playfair-display), Georgia, serif;
  font-size: clamp(30px, 4.2vw, 56px);
  font-weight: 400;
  line-height: 1.06;
  text-wrap: balance;
}

.finalCopy h2 em {
  color: var(--ff-gold-light);
}

.finalScript {
  margin: 0 0 24px;
  color: var(--ff-gold-light);
}

.finalCopy p {
  max-width: 460px;
  margin: 0 0 34px;
  color: #c6c9b6;
  font-size: 15.5px;
  font-weight: 300;
  line-height: 1.9;
  text-wrap: pretty;
}

.finalCta {
  margin-bottom: 44px;
  padding: 17px 36px;
  font-size: 12px;
}

.contactGrid {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
  gap: 20px 34px;
  padding-top: 32px;
  border-top: 1px solid rgba(217, 184, 119, 0.24);
}

.contactItem {
  display: flex;
  min-width: 0;
  align-items: center;
  gap: 14px;
  color: var(--ff-light);
  transition: color 0.25s;
}

a.contactItem:hover {
  color: var(--ff-gold-light);
}

.contactIcon {
  display: grid;
  flex: 0 0 auto;
  width: 38px;
  height: 38px;
  place-items: center;
  border: 1px solid rgba(217, 184, 119, 0.45);
  border-radius: 50%;
}

.breakText {
  min-width: 0;
  overflow-wrap: anywhere;
  word-break: normal;
}

.contactItem .breakText {
  font-size: 13.5px;
  line-height: 1.4;
}

.contactItem:first-child .breakText {
  font-size: 14px;
}

.finalMedia {
  position: relative;
  display: flex;
  min-width: 0;
  justify-content: center;
}

.finalImage {
  position: relative;
  width: min(100%, 360px);
  aspect-ratio: 1;
  overflow: hidden;
  border-radius: 46% 54% 50% 50% / 50% 46% 54% 50%;
  box-shadow: 0 26px 60px rgba(20, 26, 15, 0.34);
}

.finalImage .coverImage {
  object-position: 50% 42%;
}

.finalSeal {
  position: absolute;
  bottom: -8%;
  left: -2%;
  width: clamp(100px, 11vw, 128px);
  box-shadow:
    0 0 0 1px rgba(217, 184, 119, 0.45),
    0 14px 30px rgba(20, 26, 15, 0.3);
}

.finalSeal svg {
  animation-duration: 40s;
}

.finalSeal text {
  fill: rgba(217, 184, 119, 0.7);
  font-family: var(--font-cinzel), Georgia, serif;
  font-size: 8.6px;
  letter-spacing: 3px;
}

.finalSeal img {
  width: 52px;
  height: 52px;
  border-radius: 50%;
  object-fit: cover;
}

.footer {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
  background: var(--ff-green-dark);
  padding: 40px clamp(24px, 4vw, 56px);
}

.footerBrand {
  display: flex;
  min-width: 0;
  align-items: center;
  gap: 14px;
}

.footerBrand img {
  flex: none;
  border-radius: 50%;
}

.footerBrand strong {
  display: block;
  color: var(--ff-light);
  font-family: var(--font-cinzel), Georgia, serif;
  font-size: 13px;
  font-weight: 600;
  letter-spacing: 0.14em;
}

.footerBrand span {
  display: block;
  margin-top: 3px;
  color: #8d8a6e;
  font-size: 10.5px;
  letter-spacing: 0.14em;
}

.footerLinks {
  display: flex;
  flex-wrap: wrap;
  gap: 26px;
  min-width: 0;
}

.footerLinks a {
  color: #c6c0a8;
  font-size: 12.5px;
  transition: color 0.25s;
}

.footerLinks a:hover {
  color: var(--ff-gold-light);
}

@keyframes ffPulse {
  0%,
  100% {
    opacity: 0.45;
    transform: scale(1);
  }
  50% {
    opacity: 1;
    transform: scale(1.3);
  }
}

@keyframes ffSpin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}

@keyframes ffDown {
  0%,
  100% {
    opacity: 0.9;
    transform: translateX(0);
  }
  50% {
    opacity: 0.4;
    transform: translateX(7px);
  }
}

@keyframes ffBtnPulse {
  0%,
  100% {
    box-shadow: 0 8px 24px rgba(203, 157, 80, 0.3);
  }
  55% {
    box-shadow:
      0 12px 34px rgba(203, 157, 80, 0.5),
      0 0 0 8px rgba(203, 157, 80, 0.12);
  }
}

@media (max-width: 1040px) {
  .corporateGrid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .steps {
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    padding-bottom: 14px;
    -webkit-overflow-scrolling: touch;
  }

  .stepGroup {
    flex: 0 0 190px;
    scroll-snap-align: start;
  }
}

@media (max-width: 900px) {
  .landing {
    --ff-nav-offset: 72px;
  }

  .hero {
    min-height: 0;
  }

  .heroSticky {
    position: static;
    flex-direction: column;
    height: auto;
    padding: clamp(104px, 24vw, 132px) clamp(18px, 5vw, 28px) clamp(60px, 14vw, 96px);
  }

  .heroContent {
    opacity: 1;
    transform: none;
  }

  .heroMockup {
    position: relative;
    left: auto;
    top: auto;
    margin-top: clamp(32px, 9vw, 56px);
    opacity: 1;
    transform: none;
  }

  .heroTitleStrong,
  .heroTitleScript {
    font-size: clamp(38px, 12vw, 68px);
  }

  .twoColumn,
  .aboutGrid,
  .finalGrid {
    grid-template-columns: minmax(0, 1fr);
  }

  .testimonialsGrid {
    grid-template-columns: minmax(0, 1fr);
  }

  .testimonial + .testimonial {
    border-top: 1px solid rgba(217, 184, 119, 0.26);
    border-left: 0;
  }

  .testimonialsPanel {
    border-radius: 78px 52px 86px 58px / 46px 70px 56px 78px;
  }

  .serviceCta {
    margin-bottom: 8px;
  }

  .aboutMedia {
    max-width: min(100%, 560px);
    margin: 0 auto;
  }

  .aboutOutline {
    right: 2%;
    left: 7%;
  }

  .aboutCircleImage {
    left: 1%;
  }

  .finalGrid {
    padding-top: clamp(96px, 18vw, 136px);
  }

  .finalMedia {
    justify-content: center;
  }

  .finalSeal {
    left: max(0px, calc(50% - 210px));
  }
}

@media (max-width: 760px) {
  .nav {
    padding: 12px 14px;
  }

  .navMenu {
    display: none;
  }

  .navPill {
    justify-content: space-between;
  }

  .mockupCards {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .mockupCard:nth-child(n + 4) {
    display: none;
  }
}

@media (max-width: 720px) {
  .pathsGrid,
  .corporateGrid,
  .contactGrid {
    grid-template-columns: minmax(0, 1fr);
  }

  .pathsSection,
  .serviceSection,
  .aboutSection,
  .finalSection {
    padding-right: clamp(18px, 5vw, 24px);
    padding-left: clamp(18px, 5vw, 24px);
  }

  .pathCard {
    padding: 0 2px;
  }

  .pathCard + .pathCard {
    margin-top: 18px;
  }

  .testimonial {
    padding: 22px clamp(4px, 2vw, 16px);
  }

  .testimonial:first-child {
    padding-top: 8px;
  }

  .steps {
    flex-direction: column;
    align-items: stretch;
    overflow: visible;
    gap: 0;
    padding-bottom: 0;
    scroll-snap-type: none;
  }

  .stepGroup {
    display: block;
    flex: none;
    width: 100%;
  }

  .step {
    display: grid;
    grid-template-columns: 74px minmax(0, 1fr);
    align-items: start;
    gap: 18px;
    text-align: left;
  }

  .stepIcon {
    width: 64px;
    margin: 0;
  }

  .arrow {
    width: 64px;
    margin: 5px 0 10px;
    text-align: center;
    transform: rotate(90deg);
  }

  .scriptPhrase {
    text-align: left;
  }

  .contactGrid {
    gap: 18px;
  }

  .footer {
    align-items: flex-start;
    justify-content: center;
    text-align: center;
  }

  .footerBrand {
    justify-content: center;
  }

  .footerLinks {
    justify-content: center;
    gap: 14px 24px;
  }
}

@media (max-width: 520px) {
  .sectionHeader h2 {
    font-size: clamp(31px, 10vw, 44px);
  }

  .kicker,
  .kickerCenter {
    gap: 10px;
  }

  .kicker > span:nth-child(2),
  .kickerCenter > span:nth-child(2) {
    font-size: 10px;
    letter-spacing: 0.22em;
  }

  .kicker > span:first-child,
  .kickerCenter > span:last-child {
    width: 20px;
  }

  .heroEyebrow {
    font-size: 10px;
    letter-spacing: 0.22em;
  }

  .heroTitleStrong,
  .heroTitleScript {
    font-size: clamp(34px, 13vw, 52px);
  }

  .mockupWindow {
    height: clamp(220px, 60vw, 300px);
  }

  .pathImage {
    border-radius: 58px 44px 118px 96px / 40px 40px 86px 92px;
  }

  .testimonialsPanel {
    padding: 48px 24px;
  }

  .corporateItem,
  .featureItem {
    gap: 15px;
  }

  .aboutCircleImage {
    width: clamp(104px, 31vw, 132px);
  }

  .finalImage {
    width: min(86vw, 340px);
  }

  .finalSeal {
    bottom: -6%;
    left: 0;
    width: clamp(94px, 29vw, 118px);
  }
}

@media (max-width: 420px) {
  .nav {
    padding: 10px 10px;
  }

  .navPill {
    gap: 10px;
    padding: 8px 8px 8px 16px;
  }

  .navPillCta {
    min-height: 36px;
    padding: 0 16px;
    font-size: 10px;
    letter-spacing: 0.08em;
  }

  .heroCta {
    width: 100%;
    max-width: 320px;
    white-space: normal;
  }

  .tags span {
    max-width: 100%;
    letter-spacing: 0.1em;
  }

  .finalCta {
    width: 100%;
  }

  .footerBrand {
    flex-direction: column;
  }
}

@media (max-width: 360px) {
  .heroTitleStrong,
  .heroTitleScript {
    font-size: 30px;
  }

  .textLink {
    font-size: 10.5px;
    letter-spacing: 0.1em;
  }

  .step {
    grid-template-columns: 62px minmax(0, 1fr);
    gap: 14px;
  }

  .stepIcon,
  .arrow {
    width: 56px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .landing,
  .landing *,
  .landing *::before,
  .landing *::after {
    scroll-behavior: auto !important;
    animation: none !important;
    transition-duration: 0.001ms !important;
  }

  .landing[data-enhanced="true"] [data-reveal] {
    opacity: 1;
    transform: none;
  }

  .hero {
    min-height: 0;
  }

  .heroSticky {
    position: static;
    flex-direction: column;
    height: auto;
    padding: clamp(104px, 18vh, 140px) clamp(20px, 4vw, 40px) clamp(60px, 10vh, 100px);
  }

  .heroContent {
    opacity: 1;
    transform: none;
  }

  .heroMockup {
    position: relative;
    left: auto;
    top: auto;
    margin-top: clamp(32px, 6vh, 56px);
    opacity: 1;
    transform: none;
  }
}
